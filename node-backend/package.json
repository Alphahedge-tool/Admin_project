{
  "name": "admin-angel-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Node SmartAPI proxy for the admin Trade Panel (option chain + basket). Ported from the Go backend.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "ws": "^8.18.0"
  }
}
